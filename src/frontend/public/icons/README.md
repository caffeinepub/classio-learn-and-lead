# PWA Icons

Place the following PNG icon files in this directory:

- `icon-192.png` — 192×192 pixels (required for Android home screen)
- `icon-512.png` — 512×512 pixels (required for splash screen)

The app functions as a PWA without custom icons, but adding them improves the install experience.
